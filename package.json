{
  "name": "svg-optimization-skill",
  "version": "0.8.0",
  "private": true,
  "type": "module",
  "description": "Cross-agent SVG design skill: semantic motifs, layered effect budget, machine geometry/reference/contrast gates and a render-verified loop. Zero runtime dependencies.",
  "scripts": {
    "test": "node --test",
    "check": "node evals/grade.mjs",
    "render": "node scripts/render.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
